"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.mockPromptResolver = void 0;
var _prompts = require("../constants/prompts");
const mockPromptResolver = (query, _) => {
  const resolved = _prompts.mockPrompts.get(query.toLowerCase().trim());
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (resolved) {
        resolve(resolved);
      } else {
        console.error(`Unsupported query: ${query}`);
        reject(new Error('Could not process prompt'));
      }
    }, 1000);
  });
};
exports.mockPromptResolver = mockPromptResolver;